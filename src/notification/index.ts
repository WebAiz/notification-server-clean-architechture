import buildMakeNotification from './notification'

// add list of validation functions for the incoming inputs.
// Then pass these functions as params to buildMakeNotification function
const makeNotification = buildMakeNotification({})

export default makeNotification


